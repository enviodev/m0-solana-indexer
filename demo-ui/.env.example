# Point the demo at a deployed Envio HyperIndex GraphQL endpoint.
# If unset (or NEXT_PUBLIC_MOCK=1), the demo renders deterministic sample data.
NEXT_PUBLIC_GRAPHQL_ENDPOINT=

# Force built-in sample data even when an endpoint is configured.
# NEXT_PUBLIC_MOCK=1
